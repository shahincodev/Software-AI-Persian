# Software-AI — سیستم خودکارسازی ویندوز با هوش مصنوعی

**نسخه**: 0.9.0 (درک محیط ویندوز)  
**پلتفرم**: ویندوز 10/11 — پایتون 3.11+  
**زبان‌ها**: فارسی (اصلی)، انگلیسی  
**مجوز**: اختصاصی (تمامی حقوق محفوظ است)

---

## نمای کلی پروژه

Software-AI یک **عامل هوشمند ویندوز** است که دستورات زبان طبیعی (فارسی/انگلیسی) را دریافت کرده و با **دسترسی کامل به سیستم** آن‌ها را اجرا می‌کند. برخلاف ابزارهای خودکارسازی سنتی، این سیستم هر درخواست را از طریق یک **حلقه عامل** پردازش می‌کند:

1. دریافت درخواست کاربر
2. بارگذاری/ایجاد نشست مکالمه
3. ساخت زمینه سیستم (فایل‌ها، پوشه‌ها، درایوها، برنامه‌ها، اقدامات اخیر)
4. تصمیم‌گیری هوش مصنوعی (فراخوانی ابزار یا پاسخ متنی)
5. اجرای اقدامات سیستمی
6. بازخورد به کاربر

### نسخه 0.9.0 — درک محیط ویندوز

- **مسیریاب متمرکز**: تبدیل نام‌های طبیعی به مسیرهای فایل (Desktop, Downloads, Documents)
- **نام‌های محلی‌شده**: پشتیبانی از نام‌های فارسی و انگلیسی (دسکتاپ, دانلودها)
- **شناسایی برنامه‌ها**: کشف خودکار برنامه‌های نصب شده از رجیستری و PATH
- **اطلاعات درایوها**: نمایش برچسب، فضای کل و خالی درایوها
- **زمینه محیط**: تزریق اطلاعات محیط به پرامپت AI

### نسخه 0.8.0 — مدیریت نشست‌های مکالمه

- **ایجاد نشست**: ایجاد نشست‌های نام‌گذاری شده مانند تردهای چت
- **سوئیچ نشست**: جابجایی بین نشست‌های مختلف
- **جستجوی نشست**: جستجو در تاریخچه نشست‌ها
- **حذف نشست**: حذف نشست‌ها و پیام‌های آن‌ها
- **دستورات CLI**: `/new`, `/sessions`, `/switch`, `/delete`, `/search`, `/current`
- **ذخیره‌سازی پایدار**: تاریخچه نشست‌ها در SQLite ذخیره می‌شود

### نسخه 0.7.0 — حافظه پایدار

- **تاریخچه مکالمه**: ذخیره و بازیابی پیام‌های کاربر و دستیار در SQLite با پنجره غلتانی ۵۰ پیام
- **حافظه کوتاه‌مدت**: ذخیره‌سازی مبتنی بر TTL در حافظه برای اطلاعات موقت (۱ ساعت)
- **حافظه بلندمدت**: ذخیره‌سازی مبتنی بر SQLite با قابلیت جستجوی معنایی
- **ابزارهای هوش مصنوعی**: سه ابزار `remember`، `recall`، `forget` برای مدیریت حافظه توسط AI
- **زمینه حافظه**: تزریق خودکار اطلاعات حافظه و تاریخچه مکالمه به پرامپت AI
- **حفظ نشست**: پیام‌های کاربر و خلاصه‌های AI به‌صورت خودکار ذخیره می‌شوند

### نسخه 0.6.0 — برنامه‌ریزی چندمرحله‌ای هوشمند

- **برنامه‌ساز خودکار**: تبدیل درخواست‌های پیچیده به برنامه اجرایی ساختاریافته
- **اعتبارسنجی پلان**: بررسی خودکار امنیت، وابستگی‌ها و بهینگی قبل از اجرا
- **موتور اجرای گردش کار**: اجرای متوالی مراحل با مدیریت وابستگی‌ها
- **رهگیری پیشرفت**: نمایش لحظه‌ای پیشرفت مراحل اجرا
- **تلاش مجدد هوشمند**: در صورت شکست یک مرحله، تلاش مجدد با backoff نمایی
- **بهبود فرمان PowerShell**: تشخیص خودکار و بسته‌بندی صحیح دستورات PowerShell
- **خواندن فایل‌های بزرگ**: پشتیبانی از خواندن تکه‌ای فایل‌های بیشتر از 100KB

### نسخه 0.5.0 — شناسایی هوشمند ارائه‌دهندگان API

- **شناسایی خودکار**: سیستم به‌صورت خودکار ارائه‌دهندگان API موجود در محیط را شناسایی می‌کند
- **فقط ارائه‌دهندگان فعال**: فقط از مدل‌های ارائه‌دهندگانی استفاده می‌شود که کلید API دارند
- **لاگ‌های شفاف**: نمایش ارائه‌دهندگان شناسایی شده در ابتدای برنامه
- **خطای بهتر**: بدون تلاش‌های غیرضروری برای ارائه‌دهندگان بدون کلید
- **گزارش وضعیت**: دسترسی به اطلاعات ارائه‌دهندگان از طریق `get_provider_info()`

### نسخه 0.4.0 — حلقه بینایی خودمختار

- **مشاهده صفحه**: عامل می‌تواند صفحه را ببیند و متن OCR و عناصر قابل مشاهده را دریافت کند
- **اعتبارسنجی بصری**: بررسی خودکار نتیجه اقدامات پس از اجرا
- **تلاش مجدد هوشمند**: در صورت شکست، با زمینه بصری تلاش مجدد انجام می‌شود
- **۲۰ ابزار استاندارد**: شامل ۵ ابزار بینایی و ۲ ابزار برنامه‌ریزی جدید (execute_plan, list_plan_steps)
- **دستور screen**: مشاهده دستی وضعیت صفحه از CLI

### نسخه 0.3.0 — فراخوانی ابزار ساختاریافته

- **فراخوانی ابزار یکپارچه**: ۱۳ ابزار استاندارد با اعتبارسنجی schema
- **اعتبارسنجی خودکار**: هر فراخوانی ابزار قبل از اجرا بررسی می‌شود
- **تلاش مجدد هوشمند**: در صورت خروج نامعتبر AI، حداکثر ۲ بار تلاش مجدد با زمینه خطا
- **مسیر اجرای واحد**: ابزارها از `action_factory` → `ActionController.execute_action()` عبور می‌کنند
- **فقط fallback اضطراری**: پارسر regex فقط وقتی استفاده می‌شود که AI کاملاً شکست بخورد

### نسخه 0.2.0 — تغییرات عمده قبلی

- **معماری عامل‌محور**: هر درخواست کاربر از طریق AI با دسترسی کامل به سیستم پردازش می‌شود
- **زمینه سیستم بلادرنگ**: هوش مصنوعی فایل‌ها، پوشه‌ها و درایوهای موجود را می‌بیند
- **بازخورد حلقه‌ای**: پس از هر اجرا، نتیجه به AI گزارش شده و خلاصه‌ای به کاربر نمایش داده می‌شود
- **CLI بازنویسی شده**: از 1038 خط به ~600 خط کاهش یافته، حذف مسیریابی دستی

---

## قابلیت‌های فعلی

### آنچه امروز کار می‌کند

| قابلیت | وضعیت | توضیحات |
|--------|--------|---------|
| **حلقه عامل** | ✅ فعال | هر درخواست از طریق AI با زمینه سیستم پردازش می‌شود |
| **زمینه سیستم** | ✅ فعال | فایل‌ها، پوشه‌ها، درایوها و اقدامات اخیر به AI تزریق می‌شوند |
| **حلقه بینایی خودمختار** | ✅ فعال | مشاهده صفحه → اجرا → اعتبارسنجی بصری → تلاش مجدد |
| **اعتبارسنجی بصری** | ✅ فعال | بررسی خودکار نتیجه اقدامات پس از اجرا |
| **فراخوانی ابزار ساختاریافته** | ✅ فعال | ۲۰ ابزار استاندارد با اعتبارسنجی schema و تلاش مجدد |
| **ابزارهای بینایی** | ✅ فعال | screenshot, read_screen, find_element, verify_action, describe_screen |
| **بازخورد حلقه‌ای** | ✅ فعال | نتیجه اجرا به AI گزارش → خلاصه به کاربر نمایش داده می‌شود |
| **تحلیل هدف** | ✅ فعال | `IntentAnalyzer` فعل/هدف/اطمینان را از زبان طبیعی استخراج می‌کند |
| **برنامه‌ریزی چندمرحله‌ای** | ✅ فعال | تبدیل درخواست‌های پیچیده به برنامه اجرایی با وابستگی‌ها و رهگیری پیشرفت |
| **بینایی دسکتاپ** | ✅ فعال | عکس‌برداری از صفحه، OCR (Tesseract)، تشخیص عنصر (OpenCV) |
| **کنترل ماوس** | ✅ فعال | حرکت، کلیک، کشیدن، اسکرول از طریق Windows API |
| **کنترل صفحه‌کلید** | ✅ فعال | تایپ متن، فشردن کلیدها، کلیدهای میانبر |
| **انتظار هوشمند** | ✅ فعال | انتظار برای CPU بیکار، پنجره، ظهور عنصر، تغییر صفحه |
| **کنترلر عملیات** | ✅ فعال | اقدامات سطح بالای دسکتاپ (کلیک روی متن، پر کردن فرم و غیره) |
| **سیستم حافظه** | ✅ فعال | حافظه کوتاه‌مدت (TTL) + بلندمدت (SQLite) + تاریخچه مکالمه + ابزارهای remember/recall/forget |
| **مدیریت نشست‌ها** | ✅ فعال | ایجاد، حذف، جستجو و سوئیچ بین نشست‌های مکالمه با ذخیره‌سازی SQLite |
| **درک محیط ویندوز** | ✅ فعال | مسیریاب متمرکز، شناسایی برنامه‌ها، نمایش درایوها، نام‌های محلی‌شده |
| **تاریخچه مکالمه** | ✅ فعال | ذخیره خودکار پیام‌های کاربر و دستیار با پنجره غلتانی ۵۰ پیام |
| **زمینه حافظه** | ✅ فعال | تزریق خودکار حافظه و تاریخچه مکالمه به پرامپت AI |
| **هوش مصنوعی چندارائه‌دهنده** | ✅ فعال | زنجیره failover Groq → Gemini → OpenRouter → Ollama |
| **اقدامات سیستمی** | ✅ فعال | اجرای برنامه، نصب بسته، پرس‌وجوی سخت‌افزار، پایان فرآیند |
| **امنیت و رضایت** | ✅ فعال | امتیازدهی ریسک (۱۰۰-۰)، دروازه‌های رضایت کاربر برای اقدامات پرخطر |
| **ورودی/خروجی صوتی** | ⚠️ نسبی | تبدیل متن به گفتار کار می‌کند؛ گفتار به متن به ارائه‌دهنده بستگی دارد |
| **خودکارسازی مرورگر** | ⚠️ نسبی | یکپارچه‌سازی Playwright وجود دارد، کاملاً در مسیریابی ادغام نشده |

### محدودیت‌ها (ارزیابی صادقانه)

- **اجرای کامل سرتاسری** از زبان طبیعی → تحلیل → برنامه‌ریزی → اجرا اکنون با Phase 4 فعال شده است. درخواست‌های پیچیده به‌صورت خودکار به مراحل ساختاریافته تبدیل و با رهگیری پیشرفت اجرا می‌شوند.
- **قابلیت اطمینان مدل هوش مصنوعی** به در دسترس بودن کلید API بستگی دارد. بدون کلیدهای معتبر برای Groq/Gemini/OpenRouter، سیستم به تطبیق کلمات کلیدی بازمی‌گردد که به میزان قابل توجهی توانایی کمتری دارد.
- **کیفیت OCR** به نصب Tesseract و وضوح صفحه بستگی دارد. متن ترکیبی (فارسی + انگلیسی) می‌تواند نتایج غیرقابل اعتمادی تولید کند.
- **اجرای مبتنی بر بینایی عامل مستقل** کند است (~۲-۵ ثانیه به ازای هر عکس صفحه + تحلیل LLM) و در رابط‌های کاربری پیچیده مستعد خطا است.
- **بدون خط لوله CI/CD** — تست‌ها باید دستی اجرا شوند.
- **فقط ویندوز** — پشتیبانی بین‌پلتفرمی برنامه‌ریزی نشده است.

---

## نمای کلی معماری

### معماری عامل‌محور (نسخه 0.9.0)

```
main.py (نقطه ورودی)
    ↓
SessionManager — مدیریت نشست‌های مکالمه (ایجاد، سوئیچ، جستجو)
    ↓
SystemContext + VisionLoopManager + WindowsEnvironment — ساخت زمینه سیستم + مشاهده صفحه + درک محیط
    ↓
MemoryManager — بارگذاری تاریخچه مکالمه + حافظه کوتاه‌مدت/بلندمدت
    ↓
AIBrain.agent_chat(memory_context=...) — AI با زمینه سیستم + حافظه + وضعیت صفحه تصمیم می‌گیرد
    ├─ tool_call → اعتبارسنجی schema → تلاش مجدد در صورت خطا
    │   ├─ remember → ذخیره در حافظه بلندمدت (SQLite)
    │   ├─ recall → جستجو در حافظه بلندمدت
    │   ├─ forget → حذف حافظه
    │   └─ action_factory → ActionController → اعتبارسنجی بصری
    │       └─ بازخورد → AI خلاصه می‌سازد → کاربر می‌بیند
    ├─ execute_plan → PlanGenerator → PlanValidator → WorkflowEngine
    │   └─ StepTracker → اجرای متوالی مراحل → بازخورد
    └─ chat_reply → پاسخ مستقیم به کاربر
    ↓
ذخیره خودکار پیام‌ها در تاریخچه مکالمه (user + assistant)
```

### پشته پنج‌لایه (هسته اصلی)

```
لایه ۶ [SESSION]          SessionManager → SQLite (نشست‌های مکالمه)
لایه ۵ [UI]               main.py — نقطه ورودی مکالمه‌ای واحد
لایه ۴ [ORCHESTRATION]    IntentRouter → CapabilityManager
لایه ۳ [PLANNING]         IntentAnalyzer → PlanGenerator → PlanValidator → WorkflowEngine → MemoryIntegrator
لایه ۲ [EXECUTION]        ActionController → ExecutionManager
لایه ۱ [CAPABILITIES]     DesktopVision, MouseControl, KeyboardControl, SmartWait,
                           BrowserCore, VoiceIO, SystemTools, WindowsEnvironment

حافظه [MEMORY]            MemoryManager → ShortTermMemory (TTL) + LongTermMemory (SQLite) + ConversationHistory
```

### جریان اجرا

```
ورودی کاربر (متن/صدا)
    ↓
SessionManager — بارگذاری/ایجاد نشست مکالمه
    ↓
SystemContext.get_context() — ساخت زمینه سیستم
    ├─ فایل‌ها و پوشه‌های فعلی
    ├─ مکان‌های کلیدی ویندوز (Desktop, Downloads, Documents)
    ├─ درایوهای موجود (C:, D:, E:)
    ├─ برنامه‌های نصب شده (شناسایی خودکار)
    └─ اقدامات اخیر
    ↓
MemoryManager — بارگذاری حافظه
    ├─ تاریخچه مکالمه (آخرین ۱۰ پیام)
    ├─ حافظه کوتاه‌مدت (TTL-based)
    └─ حافظه بلندمدت (جستجوی معنایی)
    ↓
AIBrain.agent_chat(memory_context=...) — AI تصمیم می‌گیرد
    ├─ action: "tool_call"
    │   └─ ToolExecutor.execute()
    │       ├─ remember → ذخیره در حافظه بلندمدت
    │       ├─ recall → جستجو در حافظه بلندمدت
    │       ├─ forget → حذف حافظه
    │       ├─ execute_command → اجرای دستور shell
    │       ├─ launch_app → باز کردن برنامه
    │       ├─ list_directory → نمایش محتوای پوشه
    │       ├─ read_file → خواندن محتوای فایل
    │       └─ ... (ابزارهای دیگر)
    │   ↓
    │   بازخورد اجرا → AI خلاصه می‌سازد → کاربر می‌بیند
    │
    └─ action: "chat_reply"
        └─ پاسخ مستقیم متنی به کاربر
    ↓
ذخیره خودکار (user + assistant) در تاریخچه مکالمه
```

### تصمیمات کلیدی طراحی

- **عامل‌محور**: هر درخواست از طریق AI با زمینه سیستم پردازش می‌شود، نه مسیریابی قاعده‌محور.
- **زمینه بلادرنگ**: AI قبل از تصمیم‌گیری، وضعیت فعلی سیستم (فایل‌ها، پوشه‌ها، درایوها) را می‌بیند.
- **درک محیط**: سیستم به‌صورت خودکار برنامه‌ها، مسیرها و درایوها را شناسایی کرده و به AI تزریق می‌کند.
- **حافظه پایدار**: مکالمات قبلی، ترجیحات کاربر و اطلاعات مهم در SQLite ذخیره شده و در مکالمات آینده بازیابی می‌شوند.
- **مدیریت نشست**: مکالمات در نشست‌های جداگانه ذخیره شده و کاربر می‌تواند بین آن‌ها سوئیچ کند.
- **ایجاد تنبلانه**: اجزای پرهزینه (DesktopVision, BrowserCore) فقط در اولین درخواست ساخته می‌شوند.
- **بازخورد حلقه‌ای**: پس از هر اجرا، نتیجه به AI گزارش شده و خلاصه‌ای به کاربر نمایش داده می‌شود.

---

## ساختار پروژه

```
Software-AI/
├── main.py                 # نقطه ورودی واحد
├── core/
│   ├── ai_brain.py         # ارتباط با LLM (چندارائه‌دهنده) + زمینه حافظه
│   ├── action_controller.py # اجرای اقدامات دسکتاپ + سیستم (~۱۵۰۰ خط)
│   ├── action_factory.py   # کارخانه ایجاد اقدامات از دیکشنری
│   ├── action_recovery.py  # مدیریت خطا و تلاش مجدد
│   ├── action_safety.py    # بررسی‌های امنیتی پیش از اجرا
│   ├── action_types.py     # تعاریف نوع اقدام
│   ├── advanced_logging.py # سیستم لاگ پیشرفته
│   ├── agent_core.py       # ابزارهای اصلی عامل
│   ├── autonomous_agent.py # اجراگر هدف‌محور با بازخورد تصویری
│   ├── browser_core.py     # خودکارسازی وب مبتنی بر Playwright
│   ├── capability_manager.py # ثبت مرکزی، کارخانه‌ها، فعال‌سازی تنبلانه
│   ├── context_aware_actions.py # رفتار وابسته به بافت
│   ├── desktop_actions.py  # تعاریف اقدامات سطح بالای دسکتاپ
│   ├── desktop_vision.py   # عکس صفحه، OCR، تشخیص عنصر
│   ├── dialog_manager.py   # [منسوخ] صادرات مجدد از intent_analyzer
│   ├── execution_manager.py # اجراگر اقدامات سیستمی (اجرا، نصب و غیره)
│   ├── intelligent_agent.py # [منسوخ] صادرات مجدد از intent_analyzer + action_controller
│   ├── intent_analyzer.py  # استخراج هدف + گفتگو + تحلیل اقدام (~۱۴۰۰ خط)
│   ├── intent_models.py    # مدل‌های داده intent
│   ├── intent_router.py    # طبقه‌بندی مسیر، ارزیابی ریسک
│   ├── keyboard_control.py # خودکارسازی صفحه‌کلید
│   ├── logging_config.py   # زیرساخت ثبت رویداد
│   ├── logging_decorators.py # دکوراتورهای لاگ
│   ├── master_controller.py # [منسوخ] صادرات مجدد از intent_router + system_tools
│   ├── memory_integrator.py # حافظه کوتاه‌مدت/بلندمدت + تاریخچه مکالمه + یادگیری الگو
│   ├── memory_system.py    # [منسوخ] صادرات مجدد از memory_integrator
│   ├── model_config.py     # پارامترهای مدل، محدودیت‌های بافت
│   ├── model_orchestrator.py # منطق failover چندارائه‌دهنده
│   ├── monitoring_service.py # نظارت سلامت سیستم
│   ├── mouse_control.py    # عملیات ماوس
│   ├── multi_monitor.py    # پشتیبانی از چند نمایشگر
│   ├── plan_generator.py   # ایجاد برنامه اجرایی چندمرحله‌ای
│   ├── plan_validator.py   # نمره‌دهی امنیت/قابلیت اطمینان برنامه
│   ├── realtime_interpreter.py # تفسیر وضعیت اجرا
│   ├── realtime_loop.py    # اجرای بی‌درنگ با بازخورد
│   ├── safety_consent_manager.py # ارزیابی ریسک + رضایت
│   ├── safety_filter.py    # فیلتر محتوا
│   ├── session_manager.py  # مدیریت نشست‌های مکالمه (ایجاد، حذف، جستجو، سوئیچ)
│   ├── smart_wait.py       # polling هوشمند (CPU بیکار، پنجره و غیره)
│   ├── step_tracker.py     # رهگیری پیشرفت مراحل اجرا
│   ├── system_action_parser.py # پارسر اقدامات سیستمی
│   ├── system_actions.py   # تعاریف اقدامات سیستمی
│   ├── system_capabilities.py # ثبت قابلیت‌های سیستم
│   ├── system_tools.py     # عملیات سطح سیستم‌عامل
│   ├── task_engine.py      # موتور حالت وظیفه
│   ├── tool_schema.py      # ثبت ابزار یکپارچه (۱۸ ابزار) + اعتبارسنجی schema
│   ├── vision_loop.py      # حلقه بینایی خودمختار (مشاهده → اجرا → اعتبارسنجی)
│   ├── voice_io.py         # گفتار به متن، متن به گفتار
│   └── windows_environment.py # درک محیط ویندوز (مسیرها، برنامه‌ها، درایوها)
├── tests/                  # ۴۷ فایل تست
├── docs/                   # مستندات (~۶۹ فایل)
├── data/                   # داده‌های زمان اجرا (لاگ‌ها، دیتابیس SQLite، عکس‌های صفحه)
├── AI_PROJECT_RULES.md     # اصول مهندسی دائمی
├── PROJECT_MIGRATION_CONTEXT.md  # تاریخچه تحول معماری
├── AGENTS.md               # حافظه عملیاتی عامل هوش مصنوعی
├── ROADMAP.md              # نقشه راه توسعه
└── requirements.txt        # وابستگی‌های پایتون
```

---

## نصب

### پیش‌نیازها

- **سیستم‌عامل**: ویندوز ۱۰ یا ۱۱
- **پایتون**: ۳.۱۱ یا بالاتر
- **RAM**: حداقل ۴ گیگابایت
- **اینترنت**: برای دسترسی به API مدل هوش مصنوعی لازم است

### راه‌اندازی

```powershell
# 1. کلون کردن
git clone https://github.com/shahincodev/Software-AI-Persian.git
cd Software-AI-Persian

# 2. ایجاد محیط مجازی
python -m venv .venv
.\.venv\Scripts\Activate.ps1

# 3. نصب وابستگی‌ها
pip install -r requirements.txt

# 4. تنظیم کلیدهای API
Copy-Item .env.example .env
# فایل .env را با کلیدهای API خود ویرایش کنید (بخش تنظیمات را ببینید)

# 5. نصب Tesseract OCR (اختیاری، برای بینایی دسکتاپ لازم است)
# دانلود از: https://github.com/UB-Mannheim/tesseract/wiki

# 6. اجرا
python main.py
```

---

## تنظیمات

### فایل `.env`

```bash
# کلیدهای API — حداقل یکی لازم است
GEMINI_API_KEY=your_key_here
GROQ_API_KEY=your_key_here
OPENROUTER_API_KEY=your_key_here
OPENAI_API_KEY=your_key_here
ANTHROPIC_API_KEY=your_key_here
HUGGINGFACE_API_KEY=your_key_here

# مسیر Tesseract OCR (اگر در PATH نیست)
TESSERACT_PATH=C:\Program Files\Tesseract-OCR\tesseract.exe

# ثبت رویداد
LOGLEVEL=INFO
```

سیستم به‌صورت هوشمند ارائه‌دهندگان فعال را شناسایی کرده و فقط از آن‌ها استفاده می‌کند. ارائه‌دهندگان شناسایی شده در ابتدای برنامه نمایش داده می‌شوند. اگر هیچ کلید API تنظیم نشده باشد، سیستم خطا گزارش می‌کند.

### آرگومان‌های خط فرمان

```
python main.py [options]

  --input-mode {text,voice}     نوع ورودی (پیش‌فرض: text)
  --tts-provider {gtts,google-cloud,elevenlabs}
  --debug                       فعال‌سازی ثبت رویداد اشکال‌زدایی
  --dry-run                     شبیه‌سازی اقدامات بدون اجرا
  --safety-mode {safe,power}    نمایه امنیتی (پیش‌فرض: safe)
  --risk-threshold INT          آستانه ریسک ۱۰۰-۰ (پیش‌فرض: ۷۰)
  --allow-app APP               اجازه برنامه خاص (قابل تکرار)
  --allow-path PATH             اجازه مسیر خاص (قابل تکرار)
```

---

## نحوه استفاده

### تعامل پایه

```powershell
python main.py
```

درخواست‌های زبان طبیعی را تایپ کنید. سیستم آن‌ها را از طریق حلقه عامل با دسترسی کامل به سیستم پردازش می‌کند:

```
> هوای تهران چطوره؟
(AI تصمیم می‌گیرد: پاسخ متنی)

> یک پوشه روی درایو D به اسم test بساز
(AI تصمیم می‌گیرد: اجرای mkdir → بازخورد → خلاصه)

> فایل‌های Downloads من چیا هستن؟
(AI تصمیم می‌گیرد: اجرای dir → نمایش لیست)

> notepad رو باز کن و hello رو تایپ کن
(AI تصمیم می‌گیرد: اجرای اقدامات سیستمی)
```

### دستورات CLI

```
help          نمایش راهنما
clear         پاک کردن صفحه
context       نمایش زمینه سیستم فعلی
screen        نمایش وضعیت فعلی صفحه (OCR + عناصر)
history       نمایش اقدامات اخیر
pause/resume  توقف یا ادامه نشست
stop/exit     خروج از برنامه
/new [name]   ایجاد نشست جدید
/sessions     نمایش لیست نشست‌ها
/switch <id>  سوئیچ به نشست دیگر
/delete <id>  حذف نشست
/search <q>   جستجو در نشست‌ها
/current      نمایش اطلاعات نشست فعلی
```

### ورودی صوتی

```powershell
python main.py --input-mode voice
```

### حالت‌های امنیتی

- **safe** (پیش‌فرض): اقدامات پرخطر نیاز به تأیید صریح `y/n` دارند. آستانه در ۷۰.
- **power**: اعلان‌های رضایت کمتر، تحمل بیشتر برای اقدامات خودکار.

---

## محدودیت‌های فعلی

۱. **وابستگی به کلید API**: بدون کلیدهای API معتبر، سیستم نمی‌تواند از هوش مصنوعی استفاده کند.
۲. **فقط ویندوز**: متکی به Windows API (pywin32) برای مدیریت پنجره است.
۳. **بدون CI/CD**: تست‌ها باید دستی اجرا شوند.
۴. **کیفیت OCR**: به نصب Tesseract و وضوح صفحه بستگی دارد.
۵. **خودکارسازی مرورگر**: یکپارچه‌سازی Playwright هنوز کاملاً در مسیریابی ادغام نشده.
۶. **حافظه مکالمه**: محدود به ۵۰ پیام در حافظه و ۵۰۰ کاراکتر در هر پیام برای ذخیره‌سازی.

---

## نقشه راه (مرتب)

۱. **✅ Phase 1 — تثبیت خط لوله اجرا**: اعتبارسنجی مسیر Intent → Router → ActionController → Execution.
۲. **✅ Phase 2 — فراخوانی ابزار ساختاریافته**: جایگزینی پاسخ‌های چت با فراخوانی ابزار معتبر با schema.
۳. **✅ Phase 3 — حلقه بینایی خودمختار**: یکپارچه‌سازی DesktopVision، اعتبارسنجی بصری، تلاش مجدد.
۴. **✅ Phase 4 — برنامه‌ریزی چندمرحله‌ای هوشمند**: پشتیبانی از درخواست‌های پیچیده چندعملی با WorkflowEngine و StepTracker.
۵. **✅ Phase 5 — حافظه پایدار**: یادآوری مکالمات قبلی، یادگیری ترجیحات کاربر، حافظه کوتاه‌مدت و بلندمدت.
۶. **✅ Phase 6 — مدیریت نشست‌های چت**: ایجاد، حذف، جستجو و سوئیچ بین نشست‌های مکالمه.
۷. **✅ Phase 7 — درک محیط ویندوز**: تشخیص خودکار برنامه‌ها، مسیرها، پوشه‌های محلی‌شده.

---

## مستندات

### مستندات اصلی

| سند | هدف |
|-------|---------|
| `AI_PROJECT_RULES.md` | اصول مهندسی دائمی |
| `PROJECT_MIGRATION_CONTEXT.md` | تاریخچه کامل معماری، ADRها، بدهی فنی |
| `AGENTS.md` | حافظه عملیاتی عامل هوش مصنوعی و مسیریابی بافت |
| `ROADMAP.md` | نقشه راه توسعه (فاز ۱ تا ۷ — همه تکمیل شده) |

### گزارش‌های فاز

| سند | فاز | خلاصه |
|-------|---------|---------|
| `docs/PHASE2_STRUCTURED_TOOL_CALLING_REPORT.md` | Phase 2 | فراخوانی ابزار ساختاریافته با schema |
| `docs/PHASE3_AUTONOMOUS_VISION_LOOP_REPORT.md` | Phase 3 | حلقه بینایی خودمختار با مشاهده صفحه |
| `docs/PHASE4_MULTI_STEP_PLANNING_REPORT.md` | Phase 4 | برنامه‌ریزی چندمرحله‌ای هوشمند |
| `docs/PHASE5_PERSISTENT_MEMORY_REPORT.md` | Phase 5 | حافظه پایدار (کوتاه‌مدت + بلندمدت + مکالمه) |
| `docs/PHASE6_SESSION_MANAGEMENT_REPORT.md` | Phase 6 | مدیریت نشست‌های مکالمه |
| `docs/PHASE7_WINDOWS_ENVIRONMENT_REPORT.md` | Phase 7 | درک محیط ویندوز (مسیرها، برنامه‌ها، درایوها) |

### مستندات فنی

| سند | هدف |
|-------|---------|
| `docs/CLI_Architecture.md` | معماری رابط کاربری CLI |
| `docs/CLI_Environment_Restoration_Report.md` | گزارش بازسازی محیط CLI |
| `docs/AI_BRAIN_REWRITE_REPORT.md` | گزارش بازنویسی ai_brain.py — شناسایی هوشمند ارائه‌دهندگان API |
| `docs/` | ~۶۹ فایل مارک‌داون پوشش‌دهنده ماژول‌های فردی و گزارش‌های فاز |

---

## مجوز

**اختصاصی — تمامی حقوق محفوظ است.**

© ۲۰۲۵-۲۰۲۶ شاهین (shahincodev)

این نرم‌افزار "همانطور که هست" بدون هیچ گونه ضمانتی ارائه می‌شود. برای جزئیات به فایل [LICENSE](LICENSE) مراجعه کنید. برای سوالات مجوز با `shahincodev@gmail.com` تماس بگیرید.
