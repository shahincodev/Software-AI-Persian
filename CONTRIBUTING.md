# راهنمای مشارکت در پروژه

⚠️ توجه مهم دربارهٔ مجوز و مشارکت

این پروژه به‌صورت اختصاصی منتشر شده است و همهٔ حقوق متعلق به مالک آن است (All Rights Reserved). قبل از ارسال هرگونه تغییر یا Pull Request، حتماً از طریق یک issue یا ایمیل با مالک هماهنگ کنید و اجازهٔ کتبی برای مشارکت یا انتشار دریافت نمایید. اطلاعات تماس در فایل `LICENSE` قرار دارد.

از اینکه می‌خواهید در پروژه Software-AI مشارکت کنید، سپاسگزاریم! این راهنما به شما کمک می‌کند تا فرآیند مشارکت را به درستی انجام دهید.

## نسخه فعلی: 1.0.0

پروژه در نسخه 1.0.0 قرار دارد. فازهای ۱ تا ۱۰ تکمیل شده‌اند.

### ساختار پروژه

```
├── main.py                  ← نقطه ورود اصلی + CLI
├── core/
│   ├── ai_brain.py          ← هوش مصنوعی + Circuit Breaker + Cache
│   ├── model_config.py      ← پیکربندی مدل‌ها + Health Tracker
│   ├── memory_integrator.py ← حافظه + فشرده‌سازی Context
│   ├── tool_schema.py       ← تعریف ابزارها
│   └── ...
├── tests/
│   ├── test_phase8_error_resilience.py  ← 61 تست
│   └── test_phase9_integration.py       ← 13 تست
├── docs/                    ← گزارش‌های فاز
├── CHANGELOG.md             ← تاریخچه تغییرات
└── ROADMAP.md               ← نقشه راه توسعه
```

## فرآیند ارسال Pull Request

1. ابتدا یک Fork از مخزن بسازید.
2. یک شاخهٔ جدید برای تغییراتتان بسازید:
   ```bash
   git checkout -b feature/your-feature-name
   # یا
   git checkout -b fix/your-fix-name
   ```
3. تغییرات را اعمال کنید و کامیت کنید.
4. تست‌ها را اجرا کنید تا مطمئن شوید همه چیز درست کار می‌کند.
5. شاخه را به Fork خودتان push کنید.
6. یک Pull Request به شاخهٔ `main` مخزن اصلی بفرستید.

## سبک کدنویسی

- از PEP 8 پیروی کنید.
- برای همهٔ توابع و کلاس‌ها docstring بنویسید (ترجیحاً فارسی).
- از Type hints استفاده کنید.
- کامنت‌های توضیحی به فارسی بنویسید.
- حداکثر طول خط: 100 کاراکتر.

مثال:
```python
def process_task(task: str, mode: str = "browser") -> Optional[str]:
    """پردازش یک تسک و برگرداندن نتیجه.

    Args:
        task: متن تسک برای پردازش
        mode: حالت اجرا ('browser' یا 'code')

    Returns:
        نتیجهٔ پردازش یا None در صورت خطا
    """
    ...
```

## قوانین Commit

- پیام‌های commit را مختصر و مفید بنویسید
- از افعال امری استفاده کنید
- ساختار پیشنهادی:
  ```
  نوع: توضیح مختصر (حداکثر 50 کاراکتر)

  توضیحات بیشتر در صورت نیاز (هر خط حداکثر 72 کاراکتر)
  ```
- انواع رایج:
  - `feat`: قابلیت جدید
  - `fix`: رفع باگ
  - `docs`: تغییر مستندات
  - `style`: تغییرات ظاهری کد (مثل indent)
  - `refactor`: بازنویسی کد بدون تغییر رفتار
  - `test`: اضافه/ویرایش تست‌ها
  - `chore`: به‌روزرسانی‌های متفرقه

مثال:
```
feat: اضافه کردن پشتیبانی از مدل chatgpt-4

- افزودن کلاس ChatGPT4Agent
- به‌روزرسانی AIBrain برای پشتیبانی از مدل جدید
- اضافه کردن تست‌های مربوطه
```

## نکات مهم

- قبل از شروع کار، مطمئن شوید issue مرتبط وجود دارد.
- تغییرات بزرگ را ابتدا در issue پیشنهاد دهید.
- در هر PR فقط یک تغییر اصلی اعمال کنید.
- PR را کوچک نگه دارید (ترجیحاً زیر 500 خط).
- قبل از PR مطمئن شوید:
  - همهٔ تست‌ها pass می‌شوند (`python -m pytest tests/test_phase8_error_resilience.py tests/test_phase9_integration.py -v`)
  - تغییرات مستند شده‌اند
  - کد با style guide هماهنگ است

## اجرای تست‌ها

```bash
# اجرای تست‌های Phase 8 و 9
python -m pytest tests/test_phase8_error_resilience.py tests/test_phase9_integration.py -v

# اجرای تمام تست‌ها
python -m pytest tests/ -v
```

## گزارش باگ

برای گزارش باگ:
1. عنوان واضح بنویسید
2. مراحل تکرار مشکل را دقیق شرح دهید
3. رفتار مورد انتظار و رفتار فعلی را توضیح دهید
4. اطلاعات محیط اجرا را بنویسید (سیستم‌عامل، نسخهٔ پایتون و...)

## سؤالات

اگر سؤالی دارید:
- ابتدا مستندات را بخوانید
- در issues جستجو کنید
- اگر جواب را نیافتید، یک issue جدید با برچسب `question` باز کنید

با تشکر از مشارکت شما! 🙏